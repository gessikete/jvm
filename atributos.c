#include "atributos.h"

attribute_info *carregar_atributos(u2 attributes_count, u2 constant_pool_count, cp_info *constant_pool, FILE *pt_arquivo){
    attribute_info *pt_atributos = NULL;

    // TODO implementar o carregamento dos atributos

    return pt_atributos;
}
