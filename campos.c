#include "campos.h"

field_info *carregar_fields(u2 fields_count, u2 constant_pool_count, cp_info *constant_pool, FILE *pt_arquivo){
    field_info *pt_fields = NULL;

    // TODO implementar o carregamento dos fields

    return pt_fields;
}
